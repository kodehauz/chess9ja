<!-- top navigation -->
<div class="top_nav">
  <div class="nav_menu">
    <nav>
      <div class="nav toggle">
        <a id="menu_toggle"><i class="fa fa-bars"></i></a>
      </div>

      <ul class="nav navbar-nav navbar-right">
		{% if logged_in %}
		  <li><a href="{{ url('user.logout') }}"><i class="fa fa-sign-out"></i>Logout</a></li>
		  <li><a href="{{ url('user.page') }}"></a></li>
		{% else %}
		   <li><a href="{{ url('user.login') }}"><i class="fa fa-sign-in"></i>Log in</a></li>
		   <li><a href="{{ url('user.register') }}"></a></li>
		{% endif %}
      </ul>
    </nav>
    {{ page.top_nav }}
  </div>
</div>
<!-- /top navigation -->
